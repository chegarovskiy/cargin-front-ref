import React from 'react';
import { useSelector, useDispatch } from 'react-redux';

import './body.scss';

const Body = () => {

    return (
        <div className="body-wrapper">
            body
        </div>
    );
};

export default Body;